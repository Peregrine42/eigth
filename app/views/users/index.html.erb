<h1>Users</h1>

<hr>
<%= link_to "New", new_user_path %>
<hr>
<table>
  <tr><th>Name</th></tr>
  <% @resources.each do |resource| %>
    <tr><td><%= link_to resource.name, user_path(resource.id) %></td></tr>
  <% end %>
</table>
